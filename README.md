# vue-ssr-demo

Vue.js Server-Side Render Demo

Run demo guide
```shell
npm install
# build client
npm run build
# build server
npm run build:server
# start http server
npm run server
# open http://localhost:3001
```